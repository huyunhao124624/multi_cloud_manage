<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyh.netdev.dao.PrivateResourceLimitMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hyh.netdev.entity.PrivateResourceLimit">
        <id column="private_resource_limit_id" property="privateResourceLimitId" />
        <result column="user_id" property="userId" />
        <result column="cpu_limit" property="cpuLimit" />
        <result column="memory_limit" property="memoryLimit" />
        <result column="disk_limit" property="diskLimit" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        private_resource_limit_id, user_id, cpu_limit, memory_limit, disk_limit
    </sql>

</mapper>
